name: 📝 Format

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  release:
    uses: grovegs/workflows/.github/workflows/package-format.yml@v0.1.0
    with:
      files: "**/*.{json,md}"
      dotnet-projects: '["src/GroveGames.ObjectPool", "src/GroveGames.ObjectPool.Godot", "tests/GroveGames.ObjectPool.Tests", "tests/GroveGames.ObjectPool.Godot.Tests"]'
      global-json-file: global.json
