name: 🚀 Release

on:
  workflow_dispatch:
    inputs:
      version-type:
        description: Select the version to increment such as major, minor, or patch
        required: true
        default: patch
        type: choice
        options:
          - major
          - minor
          - patch

permissions:
  contents: write
  actions: read

jobs:
  release:
    name: Release Package
    uses: grovegs/workflows/.github/workflows/package-release.yml@v1.0.27
    with:
      package-name: Object Pool
      version-type: ${{ inputs.version-type }}
      runs-on: ubuntu-latest
      dotnet-project: src/GroveGames.ObjectPool
      global-json-file: global.json
      directory-build-props: Directory.Build.props
      godot-addon: src/GroveGames.ObjectPool.Godot/addons/GroveGames.ObjectPool
      unity-package: src/GroveGames.ObjectPool.Unity/Packages/com.grovegames.objectpool
      create-tag: true
      publish-github: true
      publish-nuget: true
      publish-discord: true
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}
      nuget-api-key: ${{ secrets.NUGET_API_KEY }}
      discord-webhook: ${{ secrets.DISCORD_WEBHOOK }}
