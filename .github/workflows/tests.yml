name: 🧪 Tests

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

permissions:
  contents: read

jobs:
  release:
    uses: grovegs/workflows/.github/workflows/package-tests.yml@v1.0.29
    with:
      dotnet-project: tests/GroveGames.ObjectPool.Tests
      global-json-file: global.json
