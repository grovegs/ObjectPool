name: 🧪 Tests

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

permissions:
  contents: read

jobs:
  release:
    uses: grovegs/workflows/.github/workflows/package-tests.yml@v0.1.0
    with:
      dotnet-tests: '["tests/GroveGames.ObjectPool.Tests", "tests/GroveGames.ObjectPool.Godot.Tests"]'
      global-json-file: global.json
